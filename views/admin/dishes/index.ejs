<section class="admin-section">
    <div class="container">
        <div class="section-header">
            <h1>Quan Ly Mon An</h1>
            <a href="/admin/dishes/new" class="btn btn-primary">Them Mon An Moi</a>
        </div>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success"><%= success %></div>
        <% } %>

        <% if (dishes && dishes.length > 0) { %>
            <div class="admin-card">
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>Hinh Anh</th>
                            <th>Ten</th>
                            <th>Mo Ta</th>
                            <th>Gia</th>
                            <th>Danh Muc</th>
                            <th>Giam Gia</th>
                            <th>So Luong</th>
                            <th>Trang Thai</th>
                            <th>Dat Hang</th>
                            <th>Hanh Dong</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% dishes.forEach(dish => { %>
                            <tr>
                                <td>
                                    <img src="<%= dish.image %>" alt="<%= dish.name %>" 
                                         style="width: 60px; height: 60px; object-fit: cover; border-radius: 8px;"
                                         onerror="this.src='https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=60&h=60&fit=crop'">
                                </td>
                                <td><strong><%= dish.name %></strong></td>
                                <td style="max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                                    <%= dish.description %>
                                </td>
                                <td>
                                    <% const eventDiscount = dish.event ? dish.event.discount : 0; %>
                                    <% const finalDiscount = eventDiscount || dish.discount; %>
                                    <% if (finalDiscount > 0) { %>
                                        <div>
                                            <span style="text-decoration: line-through; color: #999; font-size: 0.85rem;">
                                                <%= dish.price.toLocaleString('vi-VN') %>đ
                                            </span>
                                            <br>
                                            <strong style="color: var(--accent);">
                                                <%= (dish.price * (1 - finalDiscount / 100)).toLocaleString('vi-VN') %>đ
                                            </strong>
                                        </div>
                                    <% } else { %>
                                        <strong><%= dish.price.toLocaleString('vi-VN') %>đ</strong>
                                    <% } %>
                                </td>
                                <td>
                                    <% const categoryMap = {
                                        'appetizer': { label: 'Khai Vi', color: '#d1ecf1' },
                                        'main': { label: 'Mon Chinh', color: '#fff3cd' },
                                        'dessert': { label: 'Trang Mieng', color: '#f8d7da' },
                                        'beverage': { label: 'Do Uong', color: '#d4edda' }
                                    }; %>
                                    <% const cat = categoryMap[dish.category] || { label: dish.category, color: '#e2e3e5' }; %>
                                    <span class="category-badge" style="background: <%= cat.color %>;">
                                        <%= cat.label %>
                                    </span>
                                </td>
                                <td>
                                    <% if (dish.event) { %>
                                        <span class="discount-badge" style="background: linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%);">
                                            <%= dish.event.discount %>% (<%= dish.event.title %>)
                                        </span>
                                    <% } else if (dish.discount > 0) { %>
                                        <span class="discount-badge"><%= dish.discount %>%</span>
                                    <% } else { %>
                                        <span style="color: #999;">-</span>
                                    <% } %>
                                </td>
                                <td>
                                    <% const quantity = dish.quantity || 0; %>
                                    <span style="font-weight: 600; <%= quantity === 0 ? 'color: #dc2626;' : quantity < 10 ? 'color: #f59e0b;' : 'color: #16a34a;' %>">
                                        <%= quantity %> <span style="color: #6b7280; font-size: 0.85rem;">phan</span>
                                    </span>
                                </td>
                                <td>
                                    <% if (quantity === 0) { %>
                                        <span class="status-badge" style="background: #fee2e2; color: #991b1b;">Het hang</span>
                                    <% } else if (dish.isBestSelling) { %>
                                        <span class="status-badge" style="background: #fef3c7; color: #92400e;">Ban chay</span>
                                    <% } else { %>
                                        <span class="status-badge" style="background: #dbeafe; color: #1e40af;">Binh thuong</span>
                                    <% } %>
                                </td>
                                <td>
                                    <% const orders = dish.totalOrdersCompleted || 0; %>
                                    <span title="Tong so lan dat hang">
                                        <strong style="color: #8B6F47;"><%= orders %></strong>
                                        <span style="color: #6b7280; font-size: 0.85rem;">lan</span>
                                    </span>
                                </td>
                                <td class="action-buttons">
                                    <a href="/admin/dishes/<%= dish._id %>/edit" class="btn btn-small btn-primary">Sua</a>
                                    <a href="/admin/dishes/<%= dish._id %>/delete" class="btn btn-small btn-danger" onclick="return confirm('Ban chac chan muon xoa?')">Xoa</a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="empty-message">
                <p>Chua co mon an nao. <a href="/admin/dishes/new" class="btn btn-primary">Them ngay</a></p>
            </div>
        <% } %>
    </div>
</section>

<style>
    
    .category-badge {
        padding: 0.25rem 0.75rem;
        border-radius: 15px;
        font-size: 0.85rem;
        font-weight: 600;
        white-space: nowrap;
    }
    
    .discount-badge {
        background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
        color: white;
        padding: 0.25rem 0.75rem;
        border-radius: 15px;
        font-weight: bold;
        font-size: 0.9rem;
    }
    
    .status-badge {
        padding: 0.35rem 0.75rem;
        border-radius: 15px;
        font-size: 0.85rem;
        font-weight: 600;
        white-space: nowrap;
    }
</style>
